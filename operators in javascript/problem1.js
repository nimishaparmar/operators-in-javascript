/*
The ternary operator is similar to an if-else block. It uses three operands: a condition ,a statement if the condition is true
and a statement if the condition is false .

SYNTAX: condition ? value if TRUE : value if false;
*/

const num = 7;
const result = num % 2 === 0 ? "even" : "odd" ;
console.log (result);