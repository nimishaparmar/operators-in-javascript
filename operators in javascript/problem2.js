// The comma operator in JavaScript allows you to evaluate multiple expressions in sequence.

let a = 1, b = 2, c = 3;
console.log(a, b, c);

// In this example, the comma operator is used to declare and initialize multiple variables in a single line./